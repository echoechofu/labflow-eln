interface:
  display_name: "LabFlow Calendar"
  short_description: "Manage LabFlow experiment calendar tasks"
  default_prompt: "Use $labflow-calendar to show today's experiment tasks."

dependencies:
  tools:
    - type: "mcp"
      value: "labflow"
      description: "Local LabFlow Calendar and Task MCP server"

policy:
  allow_implicit_invocation: true
